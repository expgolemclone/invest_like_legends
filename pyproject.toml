[project]
name = "invest-like-legends"
version = "0.1.0"
description = "invest_like_legends"
requires-python = ">=3.12,<3.14"
dependencies = []

[dependency-groups]
dev = [
    "pytest>=8.0,<9",
]

[tool.pytest.ini_options]
testpaths = ["tests"]
